{
  "name": "offerflow-extension",
  "version": "0.1.0",
  "private": true,
  "description": "OfferFlow browser extension — save job postings to OfferFlow with one click.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/scrape.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^22.9.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
