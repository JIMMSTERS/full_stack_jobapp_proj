# OfferFlow Browser Extension

A browser extension (TypeScript) that captures job postings from career sites and sends
them to the OfferFlow backend.

> **Status:** Placeholder. Implementation has not started yet.

## Planned Structure

```
extension/
├── src/              # Extension source (content scripts, popup, background)
├── manifest.json     # Extension manifest (MV3)
└── README.md
```

## Notes

- Target Manifest V3 (Chrome/Edge).
- Will communicate with the backend API defined in `../backend`.
